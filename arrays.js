// JavaScript Arrays
// Colleciton of data
// usually want to make the array plural 
var tvShows = [
  'The Walking Dead', 
  'Breaking Bad', 
  'The Office'
];

console.log(tvShows[0]);

// initiated a blank array
var userSavedMovies = [];

// push method pushes values to the end of the array
userSaveMovies.push('Never Ending Story');
userSaveMovies.push('The Matrix');
userSaveMovies.push('Braveheart');

// pop will remove the last value of the array list
userSavedMovies.pop();

console.log(userSaveMovies);

//array has indexing properties which can access the value
//objects you have named properties
